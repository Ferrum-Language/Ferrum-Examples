// factorial.fe — Recursive factorial in Ferrum
#include <stdio.h>

int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}

int main() {
    int i = 0;
    for (i = 1; i <= 10; i = i + 1) {
        printf("%d! = %d\n", i, factorial(i));
    }
    return 0;
}
