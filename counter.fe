// counter.fe — Simple counter using a while loop
#include <stdio.h>

int main() {
    int i = 0;
    while (i < 5) {
        printf("count: %d\n", i);
        i = i + 1;
    }
    printf("done!\n");
    return 0;
}
